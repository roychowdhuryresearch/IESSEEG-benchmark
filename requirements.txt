# Versions here are the ones the published benchmark numbers were produced
# with (Python 3.11, CUDA 12.9). Lower bounds rather than hard pins are used
# where a package's exact version does not affect results.
#
# Install PyTorch for your own CUDA version first if 2.7.1/cu129 does not
# match your driver: https://pytorch.org/get-started/locally/

# --- deep learning ---
torch==2.7.1
torchvision==0.22.0
torchaudio==2.7.1
lightning==2.4.0
timm==1.0.15
einops>=0.8
linear_attention_transformer   # BIOT backbone
torchmetrics
deepspeed>=0.16               # LaBraM upstream training loop

# --- classical ML ---
xgboost>=3.0
scikit-learn>=1.5

# --- numerics and data ---
numpy>=2.0
scipy>=1.11
pandas>=2.2
h5py>=3.10

# --- EEG I/O ---
mne>=1.9
pyedflib

# --- plotting and utilities ---
matplotlib>=3.8
seaborn
tqdm
pyyaml>=6.0
psutil
